<%= form_with(model: admin_car, local: true) do |form| %>
  <% if admin_car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_car.errors.count, "error") %> prohibited this admin_car from being saved:</h2>

      <ul>
      <% admin_car.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :manufactuture %>
    <%= form.text_field :manufactuture, id: :admin_car_manufactuture %>
  </div>

  <div class="field">
    <%= form.label :model %>
    <%= form.text_field :model, id: :admin_car_model %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.number_field :year, id: :admin_car_year %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
